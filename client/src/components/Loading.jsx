

const LoadingComponent = () => {
    return (
        <div className="min-h-screen flex justify-center items-center">
        <span className="text-sm animate-ping text-secondary">Loading...</span>
      </div>
    );
};

export default LoadingComponent;